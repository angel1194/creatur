// import { fromJS } from 'immutable';
// import { makeSelectHotelResultDomain } from '../selectors';

// const selector = makeSelectHotelResultDomain();

describe('makeSelectHotelResultDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
