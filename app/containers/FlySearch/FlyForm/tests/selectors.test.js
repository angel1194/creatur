// import { fromJS } from 'immutable';
// import { makeSelectFlyFormDomain } from '../selectors';

// const selector = makeSelectFlyFormDomain();

describe('makeSelectFlyFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
