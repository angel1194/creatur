// import { fromJS } from 'immutable';
// import { makeSelectPasarelaDomain } from '../selectors';

// const selector = makeSelectPasarelaDomain();

describe('makeSelectPasarelaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
