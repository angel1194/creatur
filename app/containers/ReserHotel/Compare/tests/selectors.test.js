// import { fromJS } from 'immutable';
// import { makeSelectCompareDomain } from '../selectors';

// const selector = makeSelectCompareDomain();

describe('makeSelectCompareDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
