// import { fromJS } from 'immutable';
// import { makeSelectHotelsDomain } from '../selectors';

// const selector = makeSelectHotelsDomain();

describe('makeSelectHotelsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
