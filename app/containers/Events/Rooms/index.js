import React,{Component} from 'react';
import Room from '../../../components/Events/Rooms'

class Rooms extends Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div>
        <Room/>
      </div>
    );
  }
}

export default Rooms;
