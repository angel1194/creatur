// import { fromJS } from 'immutable';
// import { makeSelectRoomsDomain } from '../selectors';

// const selector = makeSelectRoomsDomain();

describe('makeSelectRoomsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
