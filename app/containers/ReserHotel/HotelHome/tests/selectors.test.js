// import { fromJS } from 'immutable';
// import { makeSelectHotelHomeDomain } from '../selectors';

// const selector = makeSelectHotelHomeDomain();

describe('makeSelectHotelHomeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
