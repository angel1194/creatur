// import { fromJS } from 'immutable';
// import { makeSelectRoomerDomain } from '../selectors';

// const selector = makeSelectRoomerDomain();

describe('makeSelectRoomerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
